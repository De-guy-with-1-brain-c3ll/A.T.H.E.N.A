# Editable ATHENA prompts

- `system_prompt.txt` controls ATHENA's speaking personality and core behavior.
- `memory_prompt.txt` controls what the background memory model retains.

Restart ATHENA after editing either file. Keep credentials out of these files.
